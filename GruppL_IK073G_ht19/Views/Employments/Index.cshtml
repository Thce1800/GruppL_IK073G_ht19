@model IEnumerable<GruppL_IK073G_ht19.Models.Employments>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.End_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Start_date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Assignments)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Persons.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.End_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Start_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Assignments)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Persons.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Employment_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Employment_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Employment_id })
        </td>
    </tr>
}

</table>
